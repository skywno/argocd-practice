apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: opa-gatekeeper-core
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "0"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: idp-control-plane
  source:
    repoURL: https://github.com/skywno/argocd-practice.git
    targetRevision: HEAD
    path: crossplane-argocd-opa/opa/core
  destination:
    server: https://kubernetes.default.svc
    namespace: gatekeeper-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 2m
    syncOptions:
      - CreateNamespace=true
