apiVersion: viaduct.ai/v1
kind: ksops
metadata:
  name: db-secrets-generator
  annotations:
    config.kubernetes.io/function: |
        exec:
          # if the binary is in your PATH, you can do
          path: ksops
files:
# List all SOPS-encrypted YAMLs that you want ksops to handle
- db-secrets-encrypt.yaml